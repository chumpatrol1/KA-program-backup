fill(0);
textSize(30);
textAlign(CENTER, CENTER);
text("Start", 200, 200);
text("Click to continue", 200, 230);

mouseClicked = function() {
    draw = function() {
        background(200);
        text("Mouse clicked " + frameCount, 200, 200);
    };
};
