var imgWidth = 100;
var imgHeight = 100;

var img = createGraphics(imgWidth, imgHeight, JAVA2D);
img.background(0, 0, 100);
img.fill(200, 0, 0);
img.ellipse(0, 30, 50, 50);

image(img, 0, 0);

var leaf = getImage("avatars/leafers-seed");
img.image(leaf, 0, -50);

image(img, 200, 200);